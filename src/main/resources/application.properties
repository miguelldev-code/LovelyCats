# =====================================
# SPRING MVC VIEW RESOLVER (JSP)
# =====================================
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp

# =====================================
# SERVER PORT (Render asigna PORT dinámico)
# =====================================
server.port=${PORT:8080}

# =====================================
# DATABASE (Render / Railway / External)
# =====================================
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABASE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# =====================================
# HIBERNATE / JPA SETTINGS
# =====================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# =====================================
# DEVTOOLS SETTINGS (opcional)
# =====================================
spring.devtools.restart.enabled=false

# =====================================
# ERROR PAGES (disable white-label)
# =====================================
server.error.whitelabel.enabled=false

# =====================================
# CHARACTER ENCODING
# =====================================
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.force=true

# =====================================
# SSL (optional, commented)
# =====================================
#server.ssl.key-store-type=PKCS12
#server.ssl.key-store=classpath:lovelycats.pfx
#server.ssl.key-store-password=79199122
#server.ssl.key-alias=LovelyCats

# =====================================
# LOCAL TEST SETTINGS (optional, commented)
# =====================================
#spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
#spring.datasource.username=miguel
#spring.datasource.password=12345
